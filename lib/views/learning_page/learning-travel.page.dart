



import 'package:flutter/material.dart';

class TravelWordPage extends StatelessWidget {
  const TravelWordPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body : Container(
        child : const Text('travel page')
      )
    );
  }
}